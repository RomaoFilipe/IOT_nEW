<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Farm Analytics</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body class="bg-gray-100 min-h-screen">

  <div class="max-w-7xl mx-auto px-4 py-6" data-controller="tabs">
    <h1 class="text-2xl font-bold text-gray-800 mb-6">Farm Analytics</h1>

    <!-- Filtros -->
    <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-4 mb-6">
      <!-- Campo -->
      <div>
        <label class="text-sm text-gray-600 mr-2">Campo:</label>
        <select id="fieldFilter" class="border rounded-md px-2 py-1 text-sm">
          <option value="all">Todos</option>
          <option value="north">Campo Norte</option>
          <option value="south">Campo Sul</option>
          <option value="east">Campo Este</option>
          <option value="west">Campo Oeste</option>
        </select>
      </div>

      <!-- Data -->
      <div>
        <label class="text-sm text-gray-600 mr-2">Data:</label>
        <input type="month" id="dateFilter" class="border rounded-md px-2 py-1 text-sm" />
      </div>
    </div>

    <!-- Navegação por tabs -->
    <div class="flex space-x-2 mb-6 overflow-x-auto whitespace-nowrap">
      <button data-tabs-target="overview" data-action="click->tabs#switch" class="tab-btn px-4 py-2 rounded-md bg-green-600 text-white shadow text-sm font-medium border">Overview</button>
      <button data-tabs-target="yield" data-action="click->tabs#switch" class="tab-btn px-4 py-2 rounded-md text-sm font-medium border">Crop Yield</button>
      <button data-tabs-target="resources" data-action="click->tabs#switch" class="tab-btn px-4 py-2 rounded-md text-sm font-medium border">Resource Allocation</button>
      <button data-tabs-target="soil" data-action="click->tabs#switch" class="tab-btn px-4 py-2 rounded-md text-sm font-medium border">Soil Conditions</button>
      <button data-tabs-target="profit" data-action="click->tabs#switch" class="tab-btn px-4 py-2 rounded-md text-sm font-medium border">Profitability</button>
    </div>

    <!-- Conteúdo das tabs -->
    <div>
      <!-- OVERVIEW -->
    <div data-tabs-content="overview">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <%= render partial: 'analytics/overview/crop_yield', locals: { chart_id: 'yieldChart_overview' } %>
        <%= render partial: 'analytics/overview/resource_allocation', locals: { chart_id: 'resourceChart_overview' } %>
        <%= render partial: 'analytics/overview/soil_conditions', locals: { chart_id: 'soilChart_overview' } %>
        <%= render partial: 'analytics/overview/profitability', locals: { chart_id: 'financialChart_overview' } %>
      </div>
    </div>

      <!-- Cada gráfico isolado -->
    <div data-tabs-content="yield" class="hidden">
      <%= render partial: 'analytics/overview/crop_yield', locals: { chart_id: 'yieldChart' } %>
    </div>
    <div data-tabs-content="resources" class="hidden">
      <%= render partial: 'analytics/overview/resource_allocation', locals: { chart_id: 'resourceChart' } %>
    </div>
    <div data-tabs-content="soil" class="hidden">
      <%= render partial: 'analytics/overview/soil_conditions', locals: { chart_id: 'soilChart' } %>
    </div>
    <div data-tabs-content="profit" class="hidden">
      <%= render partial: 'analytics/overview/profitability', locals: { chart_id: 'financialChart' } %>
    </div>

  </div>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const tabButtons = document.querySelectorAll("[data-tabs-target]")
    const tabContents = document.querySelectorAll("[data-tabs-content]")

    function switchTab(target) {
      tabContents.forEach(content => {
        content.classList.toggle("hidden", content.dataset.tabsContent !== target)
      })

      tabButtons.forEach(button => {
        const isActive = button.dataset.tabsTarget === target
        button.classList.toggle("bg-green-600", isActive)
        button.classList.toggle("text-white", isActive)
        button.classList.toggle("shadow", isActive)
        button.classList.toggle("bg-white", !isActive)
        button.classList.toggle("text-gray-700", !isActive)
      })

      // Redesenha os gráficos da tab atual após 100ms
      setTimeout(() => {
        document.querySelectorAll(`[data-tabs-content="${target}"] canvas`).forEach(canvas => {
          if (canvas.dataset.chart === "true") {
            const chartId = canvas.id
            const chartFn = window[`renderChart_${chartId}`]
            if (typeof chartFn === "function") chartFn()
          }
        })
      }, 100)
    }

    tabButtons.forEach(button => {
      button.addEventListener("click", () => switchTab(button.dataset.tabsTarget))
    })

    // Ativar tab inicial (ex: "overview")
    switchTab("overview")
  })

  function downloadChart(canvasId) {
    const canvas = document.getElementById(canvasId)
    const link = document.createElement("a")
    link.href = canvas.toDataURL("image/png")
    link.download = `${canvasId}.png`
    link.click()
  }
</script>

</body>
</html>
