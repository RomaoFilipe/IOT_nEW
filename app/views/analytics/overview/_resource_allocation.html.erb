<div class="bg-white p-4 rounded shadow">
  <h2 class="font-semibold mb-2">Resource Allocation</h2>
  <div class="relative h-64">
    <canvas id="<%= chart_id %>" data-chart="true" class="h-64"></canvas>
  </div>
</div>

<script>
  function <%= "renderChart_#{chart_id}" %>() {
    new Chart(document.getElementById('<%= chart_id %>'), {
      type: 'pie',
      data: {
        labels: ['Water', 'Fertilizer', 'Energy', 'Labor', 'Equipment'],
        datasets: [{
          data: [35, 25, 20, 15, 5],
          backgroundColor: ['#3b82f6', '#22c55e', '#facc15', '#f97316', '#a855f7']
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false
      }
    });
  }

  if (document.getElementById('<%= chart_id %>')?.closest('[data-tabs-content]:not(.hidden)')) {
    <%= "renderChart_#{chart_id}" %>();
  }
</script>
