<div class="bg-white p-4 rounded shadow">
  <h2 class="font-semibold mb-2">Alocação de Recursos</h2>
  <div class="relative h-64">
    <canvas id="<%= chart_id %>" data-chart="true" class="h-64"></canvas>
  </div>
</div>

<script>
  function renderChart_<%= chart_id %>() {
    const ctx = document.getElementById("<%= chart_id %>").getContext("2d");

    if (window["chart_<%= chart_id %>"]) window["chart_<%= chart_id %>"].destroy();

    window["chart_<%= chart_id %>"] = new Chart(ctx, {
      type: "pie",
      data: {
        labels: ["Água", "Fertilizante", "Energia", "Mão de Obra", "Equipamento"],
        datasets: [{
          data: [30, 25, 20, 15, 10],
          backgroundColor: ["#3b82f6", "#22c55e", "#f59e0b", "#f43f5e", "#a855f7"]
        }]
      },
      options: {
        responsive: true,
        plugins: {
          legend: { position: "bottom" }
        }
      }
    });
  }
</script>
